function common_courses = courses_for_majors()
% rows = ["K2:K37", "K38:K46"];

x = strcat('K', num2str(2), ':');

[num, text] = xlsread('CCC.xlsx', "B2:B32661");
for ii =1: length(num)-1
    if num(ii+1)== num(ii)
    else
        
        y = (strcat('L', num2str(ii)));
        columns = strcat(x,y);
        x = (strcat('L', num2str(ii+1), ':'));
        display(columns);
    end
end
% for ii = 1: length(rows)
%     
%     [num , text] = xlsread('CCC.xlsx', rows(ii));
%     text = unique(text);
%     text = string(text);
%     text = text.'; %transpose
%     for jj = 1:length(text)
%         common_courses(ii,jj) = text(jj);
%     end
% end
end
