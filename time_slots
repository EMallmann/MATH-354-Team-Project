function time_slots()
%open_course, open_seats, days_ofweek]
%each output will be a matrix where a row is a specific time slot
%each entry is a single class at a single time
open_course = repmat(" ", [3 100]); %class offered
open_seats = repmat(" ", [3 100]); %number of seats for that class
days_ofweek = repmat(" ", [3 100]); %days of the week for that class
rows = 2177; % lines of code in CSCD

[num, text] = xlsread('CSCD.csv', 'K:K');
eight_AM = 0.3333;
dblfind(num,eight_AM)


% for ii = 1: rows
%     kk = num2str(ii);
%     rowindx = strcat('K',ii);
%     [num , text] = xlsread('CSCD.xlsx', rowindx);
%     text = string(text);
%     text = text.'; %transpose
%     for jj = 1:length(text)
%         open_course(ii,jj) = text(jj);
%     end
% end
end
